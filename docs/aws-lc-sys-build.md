# Building `aws-lc-sys` on Windows

In order to build the `aws-lc-sys` crate on Windows, you need to have a few prerequisites installed:
- `nasm.exe`

Then run `AWS_LC_SYS_PREBUILT_NASM=1 cargo build`
