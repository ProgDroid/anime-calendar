# Project Guidelines

## Running Commands

- NPM commands need to be run from the `frontend` directory.
- Commands should be bash commands as the shell you will be using is Git for Windows.
- Use the `rm` command to delete files

## Code Preferences

- Already in Container pill in mobile view must not have the absolute positioninig of the landscape layout.
